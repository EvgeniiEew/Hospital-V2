package by.home.hospital.service.repository;

import by.home.hospital.domain.Action;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ActionJpaRepository extends JpaRepository<Action, Integer> {
}
